<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form w-[90%] flex justify-center items-center p-10">
            <form action="/create" method="POST" class="flex flex-col gap-3 w-full">
                <input name="title" type="text" placeholder="Title goes here..." name="" id="">
                <textarea name="details" placeholder="Write Your Task Description....."></textarea>
                <input type="submit" value="Add Task" class="bg-blue-500 text-white p-2 rounded-md">
            </form>
        </div>
        
        <div class="task p-10 flex flex-wrap gap-4">
            <% if (files.length > 0) { %>
              <% files.forEach((value) => { %>
                <div class="task min-w-72 rounded px-3 py-4 bg-zinc-700">
                  <h1 class="text-white text-xl pb-1"><%= value%></h1>
                  <hr>
                  <a class="text-blue-500 text-sm" href="/files/<%= value%>">Read More...</a>
                </div>
              <% }); %>
            <% } else { %>
              <h1 class="p-3 bg-gray-500 text-gray-200 rounded">No Task Yet.</h1>
            <% } %>
          </div>
          
          

    </div>

</body>
</html>